# Canonical data

Reviewed canonical data belongs in `data/corpus/<language>.jsonl`.

The MVP intentionally ships with no upstream-derived canonical corpus. Run the
importers into `../numeralform-gold-work` first, then add a review/promotion path
before committing large Gold shards.
