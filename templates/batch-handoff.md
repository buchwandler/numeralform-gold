# Numeralform Gold batch handoff

Batch: `<BATCH_ID>`

The batch is owned by its batch directory. Use the CLI status and next-packet commands to resume. Review A and B must use distinct truthful model identities. Adjudication starts only when both complete artifacts pass the review gate. Use generated role bundles for fresh contexts and merge returned JSONL artifacts atomically.

Human-facing progress should report counts, blockers, next role, and next bundle path. It should not require inspecting target-language row contents.
